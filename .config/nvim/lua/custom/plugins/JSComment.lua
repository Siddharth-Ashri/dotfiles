--[[
--  Goal: on a key press read the the line below the cursor
--      - get cursor position
--      - validate the line below the cursor is either one of:
--          / export const fnName = (...args_) => { -> stop at '{'
--          / export function fnName(...args) { -> stop at '{'
--
--]]

local function getCursorPositionAndValidateLineBelow()
    local cursorPos = 

end
